#!/bin/sh
set -e
if [ "$1" = remove ] || [ "$1" = deconfigure ]
then
	update-alternatives --remove confctl /usr/share/python-conftool/confctl
	update-alternatives --remove conftool-sync /usr/share/python-conftool/conftool-sync
fi
#DEBHELPER#
