#!/bin/sh
set -e
if [ "$1" = configure ]
then
    update-alternatives \
        --install /usr/bin/confctl confctl /usr/share/python-conftool/confctl 10
    update-alternatives \
        --install /usr/bin/conftool-sync conftool-sync /usr/share/python-conftool/conftool-sync 10
fi
#DEBHELPER#
