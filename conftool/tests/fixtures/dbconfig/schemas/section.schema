{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "Represents the metadata for a database section\n",
    "type": "object",
    "properties": {
        "master": {"type": "string", "pattern": "^([a-z]+[0-9]+)(:[0-9]+)?$"},
        "min_slaves": {"type": "integer"},
        "readonly": {"type": "boolean"},
        "reason": {"type": "string"}
    }
}
